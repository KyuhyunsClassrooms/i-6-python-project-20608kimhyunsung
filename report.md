최종 보고서: 유전자 배열 변이 분석 프로그램

1. 프로그램 주제와 목적

내 프로그램의 주제: 정상 DNA 염기서열과 환자의 DNA 염기서열을 비교하여 돌연변이(잘못된 부분)의 위치를 찾아내고, 변이율을 계산하여 환자의 상태를 판정하는 프로그램

이 프로그램을 만든 이유: 생명과학 수업 시간에 유전병과 DNA 돌연변이에 대해 배우면서, 컴퓨터 과학 기술을 활용하면 수많은 염기서열 데이터 속에서 변이를 아주 빠르고 정확하게 찾아낼 수 있다는 것을 알게 되었습니다. 정보 과목에서 배운 리스트와 반복문, 함수 구조를 실생활의 바이오인포매틱스(생물정보학) 분야에 적용해 보고 싶어 이 프로그램을 기획하였습니다.

2. 문제 분해 및 모델링

입력

정상 DNA 기준 서열 (예: "ATGCTCAGTC" - 프로그램 내부 상수 또는 기준 값)

분석 대상 환자의 이름(가명) (예: "Patient_A")

환자의 DNA 염기서열 문자열 (예: "ATGCTCGGTC")

처리

서열 길이 예외 처리: 정상 서열과 환자 서열의 길이가 일치하는지 확인합니다.

염기 비교 알고리즘: 반복문을 활용해 두 서열의 동일한 인덱스 위치에 있는 염기(A, T, G, C)를 하나씩 비교합니다.

변이 검출: 일치하지 않는 인덱스 번호, 정상 염기, 변이 염기를 기록하고 변이 개수를 누적합니다.

변이율 계산: $\text{변이율(\%)} = \frac{\text{검출된 변이 개수}}{\text{전체 DNA 길이}} \times 100$

상태 판정: 변이율에 따라 "정상"(0%), "주의"(10% 미만), "변이 발견"(10% 이상)으로 분류합니다.

출력

검출된 돌연변이의 세부 정보 (위치 인덱스, 정상 염기 -> 변이 염기)

환자의 최종 변이율 및 상태 판정 결과

분석이 완료된 전체 환자 목록 (2차원 리스트 전체 출력)

3. 사용한 2차원 리스트 설명

2차원 리스트 이름: patient_records

한 행의 의미: 한 명의 환자에 대한 기본 정보와 DNA 분석 결과 데이터 세트

각 열의 의미:

열 번호

의미

실제 예시

0번 열

환자 가명

"Patient_A"

1번 열

입력된 DNA 서열

"ATGCTCGGTC"

2번 열

계산된 변이율

10.0 (단위: %)

3번 열

최종 상태 판정 결과

"변이 발견"

이 2차원 리스트를 사용한 이유: 다수의 환자를 차례대로 입력받아 분석할 때, 환자별 이름, 서열, 변이율, 판정 결과를 하나의 묶음(행)으로 관리하고, 이를 누적하여 전체 목록(열)으로 일치성 있게 유지하기 위해 2차원 리스트 구조가 가장 적합하기 때문입니다.

4. 함수 설명

함수 이름

역할

입력값

반환값 또는 출력

analyze_dna(normal, patient)

두 서열을 비교하여 변이가 발생한 인덱스와 염기 정보를 찾음

normal (정상 서열 문자열), patient (환자 서열 문자열)

mutations (변이 정보가 담긴 2차원 리스트)

calculate_rate(error_count, total_length)

변이율을 백분율로 계산함

error_count (변이 개수), total_length (전체 길이)

계산된 실수형 변이율 (%)

get_judgment(rate)

변이율에 따른 상태 등급 판정

rate (변이율 숫자)

"정상", "주의", "변이 발견" 중 하나의 문자열

가장 중요한 함수 하나를 골라 자세히 설명하세요.

함수 이름: analyze_dna(normal, patient)

설명: 이 함수는 기준이 되는 정상 DNA 서열과 환자의 DNA 서열을 매개변수로 받습니다. 두 서열의 인덱스 i를 0부터 DNA 길이 - 1까지 변화시키며 한 글자씩 비교합니다. 만약 normal[i]와 patient[i]가 서로 다르면 돌연변이가 발생한 것으로 판단하여, 변이가 일치하지 않는 인덱스 i와 두 염기 값을 리스트 [i + 1, normal[i], patient[i]] 형태로 묶어 전체 변이 리스트에 append한 뒤 이를 반환합니다. 이 함수는 프로그램의 핵심 분석 엔진 역할을 수행합니다.

5. 조건문과 반복문 활용

조건문 사용 부분

어떤 조건을 검사했는가?

두 DNA 서열의 길이가 일치하는지 검사합니다. (if len(normal) != len(patient):)

특정 위치의 염기가 서로 다른지 검사합니다. (if normal[i] != patient[i]:)

계산된 변이율의 범위를 판정 조건으로 검사합니다. (if rate == 0:, elif rate < 10:, else:)

반복문 사용 부분

무엇을 반복 처리했는가?

DNA 서열의 길이만큼 인덱스를 순회하며 각 자리의 염기 문자를 일대일로 비교 반복하였습니다. (for i in range(len(normal)):)

전체 환자 데이터(2차원 리스트)를 차례대로 하나씩 꺼내어 화면에 보기 좋게 표 형태로 출력하는 과정을 반복하였습니다. (for row in patient_records:)

조건문과 반복문이 함께 작동한 부분

analyze_dna 함수 내부에서 for 루프를 사용해 DNA의 처음부터 끝까지 인덱스를 1씩 증가시키며 탐색하는 도중, 내부에 중첩된 if 조건을 통해 서로 다른 염기가 발견되는 순간에만 변이 정보를 수집하고 개수를 카운트하도록 설계하였습니다.

6. 실행 결과

대표 실행 결과를 붙여넣으세요.

=== 유전자 배열 변이 분석 프로그램 ===
정상 DNA 기준: ATGCTCAGTC (길이: 10)

환자 가명을 입력하세요: Patient_A
환자의 DNA 서열을 입력하세요: ATGCTCGGTC

--- 분석 결과 ---
[위치 7] 정상 염기 'A'가 환자 서열에서 'G'(으)로 변이되었습니다!

- 총 변이 개수: 1개
- 변이율: 10.0%
- 판정 결과: 변이 발견

현재까지 분석된 환자 전체 레코드 (2차원 리스트):
['Patient_A', 'ATGCTCGGTC', 10.0, '변이 발견']
======================================


결과 분석: 가상의 정상 DNA인 "ATGCTCAGTC"와 환자 "Patient_A"의 서열인 "ATGCTCGGTC"를 비교한 결과, 7번째 자리(인덱스 6)에서 기존 'A'가 'G'로 바뀐 것이 정확하게 감지되었습니다. 총 10글자 중 1글자가 변이되었으므로 변이율은 정확히 10.0%로 계산되었고, 기준에 따라 "변이 발견" 판정이 성공적으로 출력되었습니다.

7. 테스트와 오류 수정

테스트 1: 정상 사례

입력:

환자명: "Healthy_User"

DNA 서열: "ATGCTCAGTC" (정상 서열과 완벽히 일치)

예상 결과: 변이 개수 0, 변이율 0.0%, 판정 결과 "정상"

실제 결과: 예상대로 변이가 검출되지 않았으며 판정 결과가 "정상"으로 올바르게 출력됨.

테스트 2: 예외 또는 경계 사례

입력:

환자명: "Error_User"

DNA 서열: "ATGC" (정상 서열보다 짧은 입력값)

예상 결과: "정상 서열과 길이가 다릅니다. 다시 입력하세요."라는 경고 메시지 출력 후 재입력 대기 혹은 종료.

실제 결과: 인덱스 범위 초과 오류(IndexError)가 발생하며 프로그램이 강제 종료됨.

발견한 오류와 수정한 방법

발견한 오류: 환자가 실수로 정상 DNA 서열과 다른 길이의 서열을 입력했을 때, 비교 반복문(for i in range(len(normal))) 작동 시 환자 서열의 인덱스 범위를 벗어나며 IndexError가 발생해 프로그램이 멈추는 취약점이 발견되었습니다.

수정 방법: 분석 함수를 호출하기 전에 두 서열의 길이를 비교하는 방어적 코드(조건문)를 추가하였습니다. 길이가 다를 경우 "서열 길이가 일치하지 않습니다. 다시 확인해 주세요."라는 메시지를 출력하고 분석 단계를 건너뛰도록 구조를 개선하여 프로그램의 안정성을 높였습니다.

8. Gemini 활용 내용 요약

Gemini에게 도움받은 부분:
막연하게 생각했던 유전자 분석이라는 주제를 고등학교 수준의 2차원 리스트와 사용자 정의 함수 분리 조건에 맞게 단순화하고, 입력-처리-출력 구조의 틀을 설계하는 데 큰 도움을 받았습니다.

Gemini가 제안했지만 내가 수정하거나 사용하지 않은 부분:
Gemini는 여러 명의 환자 서열을 미리 소스코드 안에 고정값(하드코딩)으로 대입해 두는 방식을 제안했지만, 저는 사용자의 실제 인터랙션을 늘리기 위해 input() 함수를 통해 실시간으로 새로운 환자 정보를 입력받아 2차원 리스트에 추가(append)해 나가는 동적인 방식으로 수정하여 구현했습니다.

최종 코드에서 내가 직접 판단하거나 수정한 부분:
잘못된 서열이 입력되었을 때 프로그램이 비정상적으로 꺼지지 않도록 하는 예외 처리 조건문을 직접 추가하였으며, 판정 결과의 기준 범위를 생명과학 교과서의 변이율 정보에 기반하여 구체적인 등급 수치로 재설정하였습니다.

9. 한계와 개선 방안

현재 프로그램의 한계:
현재 프로그램은 오직 두 서열의 1:1 글자 매칭(치환 변이)만 잡을 수 있습니다. 유전자 변이에는 염기가 중간에 삽입되거나 누락되는 결실/삽입 변이도 존재하는데, 이 경우 서열 전체가 밀리게 되어 프로그램이 모든 자리를 에러로 인식하는 한계가 있습니다.

다시 개선한다면 추가하고 싶은 기능:
서열의 삽입과 결실을 인지할 수 있는 '정렬 알고리즘(Sequence Alignment)'의 개념을 아주 간단한 수준으로 도입해 보고 싶고, DNA 서열을 3글자씩 묶어 아미노산 코돈 테이블로 번역해 최종 단백질에 미치는 영향까지 분석해 주는 고도화된 예측 기능을 탑재해 보고 싶습니다.

10. 코드인터뷰 준비

1. 내 프로그램의 입력-처리-출력은 무엇인가?

환자 이름과 DNA 서열 문자열을 입력받아, 정상 서열과의 일대일 인덱스 비교 및 변이율 수식 처리를 거쳐, 발견된 변이 위치 정보와 최종 상태 판정 결과 및 전체 환자 현황 리스트를 출력합니다.

2. 2차원 리스트에서 한 행과 각 열은 무엇을 의미하는가?

한 행은 '한 명의 환자 정보 데이터셋'을 의미하며, 각 열은 순서대로 '0번 열: 환자 이름', '1번 열: DNA 서열', '2번 열: 변이율', '3번 열: 분석 결과 등급'을 뜻합니다.

3. 가장 중요한 함수는 무엇이고 어떤 역할을 하는가?

analyze_dna 함수가 가장 중요합니다. 정상 서열과 환자 서열을 인덱스 기준으로 한 글자씩 매핑하며 일치하지 않는 돌연변이의 세부 인덱스와 해당 염기 정보를 리스트로 가공해 반환하는 핵심 필터링 역할을 담당합니다.

4. 조건문과 반복문은 어디에서 사용되었는가?

DNA 서열의 각 염기를 처음부터 끝까지 비교하기 위해 for 반복문을 썼으며, 특정 자리가 서로 일치하지 않는지 판정하고 변이율 수치에 따라 등급을 나눌 때 if-elif-else 조건문을 긴밀하게 사용했습니다.

5. Gemini가 도와준 부분과 내가 직접 수정한 부분은 무엇인가?

문제의 뼈대를 고등학교 파이썬 문법 수준에 맞게 추상화하는 모델링 작업을 Gemini에게 도움받았으며, 입력 데이터의 길이 에러 검증 코드를 추가하고 실제 사용자가 터미널에서 입력을 주도적으로 수행할 수 있도록 제어 구조를 커스텀한 것은 제가 직접 구현하였습니다.