# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제: **DNA 염기서열 분석 및 돌연변이 탐지기**

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

내 설명:
의생명 분야에서 환자의 유전병을 진단하거나 유전적 변이를 연구할 때, 정상 DNA 염기서열과 환자의 DNA 염기서열을 수작업으로 일일이 비교하는 것은 시간이 오래 걸리고 실수가 생기기 쉽습니다. 따라서 컴퓨터 프로그램을 통해 두 DNA 염기서열을 자동으로 비교하여 돌연변이가 발생한 정확한 위치와 염기 정보를 찾아내고, 전체 변이율을 계산하여 유전질환의 위험도를 빠르게 판정하고자 합니다.

---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 비교 기준이 되는 정상 DNA 염기서열 (문자열)
- 검사 대상이 되는 환자의 DNA 염기서열 (문자열)

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 두 DNA 서열의 길이가 같은지 비교하여 다르면 오류 메시지를 출력합니다.
- 반복문을 사용하여 서열의 인덱스를 0번부터 끝까지 이동하며 정상 DNA와 환자 DNA의 글자를 비교합니다.
- 두 글자가 다르면 돌연변이로 판단하고, 해당 위치(인덱스+1), 정상 염기, 환자 염기를 2차원 리스트에 추가합니다.
- (발견된 돌연변이 개수 / 전체 DNA 길이) * 100 공식으로 변이율(%)을 계산합니다.

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

- 발견된 모든 돌연변이의 상세 정보 목록 (위치, 정상 염기 -> 변이 염기)
- 최종 DNA 변이율(%) 수치
- 변이율이 기준치(10%) 이상일 때 출력되는 위험 경고 메시지 또는 안전 안내 메시지

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름: `mutations`

### 한 행의 의미
정상 DNA와 환자 DNA를 비교했을 때 일치하지 않는 '돌연변이 1개에 대한 상세 정보'를 의미합니다.

### 각 열의 의미

| 열 번호 | 의미 | 예시 |
|---:|---|---|
| 0번 열 | 돌연변이가 발견된 서열상의 위치 (1부터 시작하는 순번) | `3` |
| 1번 열 | 정상 DNA의 해당 위치 염기 (A, T, G, C) | `"C"` |
| 2번 열 | 환자 DNA의 해당 위치 염기 (A, T, G, C) | `"G"` |

---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |
|---|---|---|---|
| `show_intro` | 프로그램의 이름과 목적을 화면에 안내한다. | 없음 | 안내 문구 출력 |
| `get_dna_sequences` | 사용자로부터 분석할 두 서열을 대문자로 입력받는다. | 없음 | 정상 서열, 환자 서열 문자열 반환 |
| `analyze_dna` | 두 서열을 비교해 돌연변이를 찾아 2차원 리스트로 모은다. | 정상 서열, 환자 서열 | 돌연변이 정보가 담긴 2차원 리스트 |
| `print_result` | 2차원 리스트를 바탕으로 상세 정보, 변이율, 질환 위험도를 출력한다. | 결과 2차원 리스트, 전체 서열 길이 | 상세 내역 및 변이율, 경고문 출력 |

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분

내 계획:
1. 두 서열의 길이가 일치하는지 확인할 때 (`len(normal) != len(patient)`) 사용합니다.
2. 반복문 내부에서 특정 위치의 두 염기가 서로 다른지 판단할 때 (`normal[i] != patient[i]`) 사용합니다.
3. 최종 변이율이 위험 기준치인 10% 이상인지 판정할 때 (`mutation_rate >= 10.0`) 사용합니다.

### 반복문을 사용할 부분

내 계획:
1. DNA 서열의 길이만큼 인덱스를 하나씩 증가시키며 모든 염기를 순서대로 대조할 때 (`for i in range(len(normal))`) 사용합니다.
2. 발견된 돌연변이들이 저장된 2차원 리스트에서 한 행씩 데이터를 꺼내어 화면에 줄 바꿈을 하며 출력할 때 (`for item in results`) 사용합니다.

---

## 7. 예상되는 예외 상황

1. 사용자가 입력한 정상 DNA 서열의 길이와 환자 DNA 서열의 길이가 서로 다를 때 (예: 하나는 4글자, 하나는 6글자 입력)
2. 두 서열이 완벽하게 일치하여 돌연변이가 0개(2차원 리스트가 비어있음)인 상황이 발생할 때

---

## 8. Gemini에게 도움받고 싶은 부분

1. 의생명 도메인에 맞는 적절한 2차원 리스트 활용 데이터 구조 설계
2. 입력-처리-출력 흐름에 따른 효율적인 함수 분리 방식 정의
3. 서열 분석 중 발생할 수 있는 글자 길이 불일치 예외 처리 구현 힌트